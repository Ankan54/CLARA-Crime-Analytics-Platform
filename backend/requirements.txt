fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
sqlalchemy>=2.0.30
psycopg[binary]>=3.2.1
requests>=2.32.0
zcatalyst-sdk>=1.4.0
rapidfuzz>=3.9.0
# All langchain/langgraph lines are capped <1.0.0 deliberately -- do not "upgrade" them.
# langchain-core>=1.0 depends on langchain-protocol, which needs typing-extensions>=4.13,
# but zcatalyst-sdk 1.4.0 (the newest release; checked) hard-pins typing-extensions
# ~=4.12.1. Those ranges never overlap, and pip aborts the ENTIRE file on one
# unresolvable line -- so this file previously installed *nothing at all*, which is why
# langchain-openai was missing locally despite being listed. An earlier attempt to force
# typing-extensions>=4.13.0 here could never have worked: pip reports
# ResolutionImpossible rather than picking a side. Capping <1.0 keeps langchain-protocol
# out of the graph entirely and sidesteps the conflict. To move to langchain-core>=1.0,
# zcatalyst-sdk's pin has to give first.
langgraph>=0.2.0,<1.0.0
langchain-core>=0.3.0,<1.0.0
langchain-openai>=0.2.0,<1.0.0
langchain-anthropic>=0.2.0,<1.0.0
langchain-aws>=0.2.0,<1.0.0
neo4j>=5.24.0
pinecone>=5.0.0
boto3>=1.34.0
xhtml2pdf>=0.2.16
jinja2>=3.1.0
markdown>=3.6
pypdf>=4.2.0
python-docx>=1.1.0
beautifulsoup4>=4.12.0
# Agent code-execution artifacts (charts + tabular analysis via the run_python tool).
matplotlib>=3.8.0
pandas>=2.2.0
plotly>=5.24.0
# Polished HTML->PDF. WeasyPrint is primary (needs Pango/Cairo on Linux/Docker);
# xhtml2pdf above stays as the Windows/native fallback when system libs are absent.
weasyprint>=62.0
