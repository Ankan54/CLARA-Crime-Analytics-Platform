# NOT vendored — the whole point is to test whether `catalyst deploy` installs these
# (and a complete botocore/data tree) server-side, unlike manual zip upload.
boto3>=1.34.0
langchain-aws>=0.2.0,<1.0.0
langchain-core>=0.3.0,<1.0.0
